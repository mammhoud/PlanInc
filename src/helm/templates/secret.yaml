apiVersion: v1
kind: Secret
metadata:
  name: {{ include "planinc.fullname" . }}-secret
  labels:
    {{- include "planinc.labels" . | nindent 4 }}
type: Opaque
data:
  NEXTAUTH_SECRET: {{ .Values.config.nextauth.secret | b64enc | quote }}
---
{{- if and .Values.externalDatabase.enabled (not .Values.externalDatabase.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "planinc.fullname" . }}-db-secret
  labels:
    {{- include "planinc.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.externalDatabase.password | b64enc | quote }}
{{- end }} 