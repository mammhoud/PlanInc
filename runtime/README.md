# `runtime`

> Part of **PlanInc** — AI-powered card note-taking and planning

The canonical deployed application: a dependency-light Node HTTP server (`server.mjs`) that serves the PlanInc UI and API on top of an embedded SurrealDB (`surrealkv://`) store. This is what the `planinc` container runs.

## Contents

- `context/` - 0 files
- `public/` - 26 files
- `tests/` - 8 files
- `.dockerignore`
- `Dockerfile`
- `package-lock.json`
- `package.json` - package/tooling manifest
- `playwright.canonical.config.mjs`
- `playwright.config.mjs`
- `playwright.extra.config.mjs` - Dedicated config for suites that must not share the main server: the
- `server.mjs`
- `walkthrough.tmp.mjs`

## Public API

- _(no barrel export - import the individual modules listed in Contents)_

## Usage

```bash
cd runtime && npm test          # drive the runtime through Playwright
```

---
*Auto-generated by `scripts/generate-dir-docs.py`*
