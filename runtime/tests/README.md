# `runtime/tests`

> Part of **PlanInc** — AI-powered card note-taking and planning

Playwright integration suite. Specs drive the real server over HTTP, so they assert behaviour rather than mocks.

## Contents

- `fixtures/` - 2 files
- `appearance-and-graph.spec.mjs` - Regression coverage for the Tier-1 appearance tokens and the graph
- `auth-and-categories.spec.mjs` - Tests 1..N share one seeded admin. Playwright restarts the worker process
- `canonical-planinc.spec.mjs` - Smoke coverage for an already-running deployment (docker compose, or
- `i18n-parity.spec.mjs` - Translation completeness is a build-time property, not a UI behaviour, so it
- `mock-llm.mjs` - Minimal OpenAI-compatible mock server used by the Playwright suite to test
- `tickets.spec.mjs` - Support tickets: custom field definitions, ticket CRUD through the modal

## Public API

- _(no barrel export - import the individual modules listed in Contents)_

## Usage

```bash
cd runtime && npm test          # drive the runtime through Playwright
```

---
*Auto-generated by `scripts/generate-dir-docs.py`*
