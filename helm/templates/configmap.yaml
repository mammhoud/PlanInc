apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "planinc.fullname" . }}-config
  labels:
    {{- include "planinc.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.config.nodeEnv | quote }}
  NEXTAUTH_URL: {{ .Values.config.nextauth.url | quote }}
  NEXT_PUBLIC_BASE_URL: {{ .Values.config.baseUrl | quote }} 